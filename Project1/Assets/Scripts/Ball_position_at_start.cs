using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ball_position_at_start : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        transform.position = new Vector3((float)4.3, (float)2.6, (float)-67.8);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
